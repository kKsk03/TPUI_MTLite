<Window x:Class="TeknoParrotUi.Views.UpdateWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:local="clr-namespace:TeknoParrotUi.Views"
            mc:Ignorable="d"
            Title="正在更新......" Height="150" Width="350" WindowStartupLocation="CenterScreen" SizeToContent="WidthAndHeight" ResizeMode="NoResize" Topmost="True">
    <Grid Margin="10" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- 游戏目标版本 -->
        <TextBlock
            x:Name="TargetGameVersion"
            Text="正在更新至：Rev3.05.03"
            FontSize="24"
            FontWeight="Bold"
            TextAlignment="Center"
            HorizontalAlignment="Center"
            Margin="0,0,0,5"
            Grid.Row="0" />

        <!-- 当前更新文件 -->
        <TextBlock
            x:Name="CurrentUpdatingFile"
            Text="正在更新：xxxx"
            FontSize="12"
            TextAlignment="Center"
            HorizontalAlignment="Left"
            Grid.Row="1" />

        <!-- 更新进度条 -->
        <ProgressBar
            x:Name="UpdateProgressBar"
            Height="18"
            Minimum="0"
            Maximum="100"
            Value="0"
            HorizontalAlignment="Stretch"
            Margin="0,2,0,0"
            Grid.Row="2" />
    </Grid>
</Window>